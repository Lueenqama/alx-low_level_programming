#include <stdio.h>

/**
 * main - Entry point
 * Description: 'print alphabets except q and e'
 * Return: Always 0
 */
int main(void)
{
	int l = 97;

	for (l = 0; l <= 9; l++)
	{
		printf("%d", l);
	}
	printf("\n");
	return (0);
}
